# ── 大型数据集（不上传 GitHub）──────────────────────────────
Drone RF Data/
rf_yolo_dataset/

# ── 临时目录 ──────────────────────────────────────────────
OrangepiVersion/
rknn_env/

# ── YOLO 训练产物与模型权重 ────────────────────────────────
runs/
*.pt
*.onnx

# ── 运行时产物（数据库 & 告警截图）────────────────────────
database/rf_alert_history.db
database/alert_images/
diagnostics/captures/
diagnostics/*.tar.gz

# ── Python 缓存 ──────────────────────────────────────────
__pycache__/
*.pyc
*.pyo

# ── Windows 开发工具（不需要入库）──────────────────────────
*.bat

# ── 现场校准产物（每台设备独立，不入库）────────────────────
rf_zynq/s3_thresholds.json
rf_zynq/*.bak

# ── Renesas e2 studio / RA8P1 构建产物 ─────────────────────
firmware_ra8p1/e2_project/**/Debug/
firmware_ra8p1/e2_project/**/build/
firmware_ra8p1/e2_project/**/*.log
firmware_ra8p1/e2_project/**/*.elf
firmware_ra8p1/e2_project/**/*.map
firmware_ra8p1/e2_project/**/*.srec
firmware_ra8p1/e2_project/**/*.rpd
firmware_ra8p1/e2_project/**/*.cref
firmware_ra8p1/e2_project/**/*.sbd
